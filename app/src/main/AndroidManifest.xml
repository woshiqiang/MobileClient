<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobileclient.activity"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />

    <application
        android:name="com.mobileclient.app.Declare"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="MainMenuActivity"></activity>
        <activity android:name="BookClassListActivity"></activity>
        <activity android:name="BookClassAddActivity"></activity>
        <activity android:name="BookClassEditActivity"></activity>
        <activity android:name="BookListActivity"></activity>
        <activity android:name="BookQueryActivity"></activity>
        <activity android:name="BookAddActivity"></activity>
        <activity android:name="BookEditActivity"></activity>
        <activity android:name="BookDetailActivity"></activity>
        <activity android:name="photoListActivity"></activity>
        <activity android:name=".RegisterActivity" />
    </application>


    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 文件读写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

</manifest>